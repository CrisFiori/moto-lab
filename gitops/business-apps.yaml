# gitops/business-apps.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: business-apps
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/CrisFiori/moto-lab.git"
    targetRevision: HEAD
    path: gitops/business-apps
    directory:
      recurse: true
  destination:
    server: "https://kubernetes.default.svc"
    namespace: moto-catalog
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
